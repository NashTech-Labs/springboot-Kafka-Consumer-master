server:
  port: 8091
spring:
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: the-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: com.knoldus.spring.kafka.events.deserializer.DataMessageDeserializer
